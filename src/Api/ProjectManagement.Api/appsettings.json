{
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=./data/app.db;Cache=Shared"
  },
  "Jwt": {
    // NOTE: For production, store this in secure secret storage (Azure Key Vault, environment variable, etc.)
    // Must be at least 32 characters (256 bits) for HMAC-SHA256 signing
    "Key": "change-this-to-a-secure-32-char-min-key!!",
    "Issuer": "ProjectMgmtApi"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  }
}
